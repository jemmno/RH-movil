export interface ResumenExtensionPorTipoEvento {
    codtipo: number;
    descripcion: string;
    cantidad: number;
    maxhoras: number;
    creditos: number;
    ajustado: number;
    tipoActividad: string;
}